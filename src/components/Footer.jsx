export default function Footer(){

  const footerStyle ={
    textAlign: "center",
    fontSize: "1.5rem"
  }

  return(
    <footer>
      <p style={footerStyle}>Estamos abiertos</p>
    </footer>
  )
}